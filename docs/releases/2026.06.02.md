- Update name to Wisemonkey
- Add methods to structure error, info, and warn printouts
- Do not crash when chat memory is empty
- Add `/session chat` command
- Add vector store support to embed documents to ChromaDB using the new `/embed` command
- Add `query_vectorstore` tool for LLMs to query the vector store
- Sort models by name in models list
- Use Ctrl+c to clear, double tap to quit
- Capture Ctrl+Shift+v (paste) to add pasted contents as files in the session directory
- Enable `.env` file to be located in config directory `~/.config/wisemonkey/.env`
- Better statusline
- Add `--edit-config` CLI argument and `/config edit` command to edit configuration file
